# ignore dependency caches
node_modules/
.yalc/
yalc.lock

# ignore builds
dist/

# ignore ratchet-file.json generated by the build
bundle-size-ratchet.json

# ignore package.json generated by the build
packages/*/*/*/package.json

# ignore generated files
coverage/
**/.netlify
junit.xml
test-reports/
browserstack.err
integration_tests_artifacts/*
**/__image_snapshots__/__diff_output__*/
**/__image_snapshots__/__errors__*/
.measure-output/
.masterBundleSize/
.currentBundleSize/
imageSnapshotFailures/
.componentlab/
traces/
tmp/

# generated only in CI, but might accidentally exist locally
build-info.json
changed-packages-info.json
*-integrator-variables.json
release-info.json

# created by build-stats tool
.data/

# ignore yarn.locks generated in packages
packages/**/**/yarn.lock

# ignore all logs
*.log

# ignore editor generated files
.vscode/*
!.vscode/extensions.json
.idea
.iml
*.code-workspace

# ignore OS generated files
**/.DS_Store

# We need to write out this alias for running the constellation website, however committing it causes issues
services/website-constellation/aliases-written-map.json

# This is a hardcoded file for now, just to show that things are working, will remove once not
# hardcoded
releases/new-release.md
website/dist.zip

website/public/stats.json
website/public/report.html
website/public/output/*

# A file we use for pushing secrets to micros, just in case
secrets.json
.vs/slnx.sqlite

# this is created only in deploy-branch-build-dists builds in CI
dists/

# allow using .envrc to automate local environment, exclude from CVS
.envrc

# dotenv environment-specific configuration
.env

# override local config which for many atlassians will be set to registry=https://packages.atlassian.com/api/npm/npm-remote and hence cause certain issues with package resolution
.npmrc
nanos-env.json

# generated tarballs
*.tgz
*.tar.gz

# File is temporarily generated for running subsets of packages
.storybook/preview.js

# Ignore repo-docs spelling file and yarn.lock
repo-docs/.spelling
repo-docs/yarn.lock

# Ambient build declaration
**/src/ambient.d.ts

# generated cypress assets
cypress/videos/
cypress/screenshots/

# node_modules cache
node_modules.tar.lz4

# generated directory when running repo-stats
backfill

# Logs
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# Temp file
tsconfig.entry-points.tmp.json
