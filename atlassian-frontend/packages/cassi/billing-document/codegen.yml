overwrite: true
schema:
    - 'http://localhost:8080/api/1/graph'
documents: 'src/ui/invoice/*.graphql'
generates:
    src/common/utils/graph-types.ts:
        plugins:
            - 'typescript'
            - 'typescript-operations'
        config:
            namingConvention:
                enumValues: keep
