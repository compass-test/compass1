import styled from '@emotion/styled';

import { IconWrapper } from '../../common/ui/styled';

export const DeferredIconWrapper = styled(IconWrapper)`
  max-width: 40px; // fix max width of the icon
`;
