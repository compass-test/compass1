import styled from 'styled-components';

// Wrapper for smart links to show as select options in dropdown.
export const SmartLinkWrapper = styled.div`
  background-color: transparent;
  a {
    box-shadow: none;
    border-radius: 0;
    background-color: transparent;
  }
`;
