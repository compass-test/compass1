mutation addComponentLabels($input: AddCompassComponentLabelsInput!) {
  compass {
    addComponentLabels(input: $input) {
      success
      addedLabels {
        name
      }
      errors {
        ...CommonMutationError
      }
    }
  }
}
