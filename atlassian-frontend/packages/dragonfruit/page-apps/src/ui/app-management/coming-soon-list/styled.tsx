import styled from 'styled-components';

import { gridSize } from '@atlaskit/theme/constants';

export const ComingSoonListWrapper = styled.div`
  display: grid;
  grid-auto-flow: column;
  grid-gap: ${gridSize()}px;
`;
