import styled from 'styled-components';

import { gridSize } from '@atlaskit/theme/constants';

export const ForgeAppIcon = styled.img`
  width: ${gridSize() * 3}px;
  height: ${gridSize() * 3}px;
`;
