import styled from 'styled-components';

import { gridSize } from '@atlaskit/theme/constants';

export const InlineEditWrapper = styled.div`
  margin-left: -${gridSize()}px;
  padding-bottom: ${28 * gridSize()}px;
`;
