import styled from 'styled-components';

import { gridSize } from '@atlaskit/theme/constants';

export const RelationshipListContainer = styled.div`
  width: 100%;
`;

export const DataManagerLinkWrapper = styled.div`
  margin-top: ${gridSize()}px;
`;
