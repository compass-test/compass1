import { defineMessages } from 'react-intl';

export default defineMessages({
  componentFetchErrorMessage: {
    id:
      'dragonfruit-page-team-details.add-component-modal.component-options-fetch-error-message',
    defaultMessage: "We couldn't load the component list.",
    description:
      'Message shown in team select picker when an error occurs while fetching team options',
  },
});
