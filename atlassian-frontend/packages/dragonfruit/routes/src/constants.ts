// Strings prefixed with this are an ARI
export const ARI_PREFIX = 'ari:cloud:';

// Strings are prefixed with this after being base64 decoded
// Use this to determine if it needs to be decoded or not
export const BASE_64_ENCODED_PREFIX = 'b:';
