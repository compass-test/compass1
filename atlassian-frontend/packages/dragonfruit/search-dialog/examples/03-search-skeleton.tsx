import React from 'react';
import { ProductSearchInputSkeleton, Props } from '../src/skeleton';

const SearchSkeleton = (props: Props) => {
  return <ProductSearchInputSkeleton {...props} />;
};

export default SearchSkeleton;
