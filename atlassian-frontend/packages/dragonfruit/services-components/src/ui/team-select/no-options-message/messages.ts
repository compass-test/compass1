import { defineMessages } from 'react-intl';

export default defineMessages({
  teamsFetchErrorMessage: {
    id:
      'dragonfruit-services-components.team-select.team-options-fetch-error-message.nonfinal',
    defaultMessage: "We couldn't load the team list.",
    description:
      'Message shown in team select picker when an error occurs while fetching team options',
  },
});
