import { run as appWithModalDialog } from './AppWithModalDialog';
import { run as appWithInlineDialog } from './AppWithInlineDialog';
import { run as selectedTextApp } from './SelectedTextApp';
import { run as spaceInfoApp } from './SpaceInfoApp';
import { run as bylineApp } from './BylineApp';
import { run as worldTimeApp } from './WorldTimeApp';
import { run as spaceSettingsApp } from './SpaceSettingsApp';

export const apps = {
  appWithModalDialog,
  appWithInlineDialog,
  bylineApp,
  selectedTextApp,
  spaceInfoApp,
  spaceSettingsApp,
  worldTimeApp,
};

export {
  appWithModalDialog,
  appWithInlineDialog,
  bylineApp,
  selectedTextApp,
  spaceInfoApp,
  spaceSettingsApp,
  worldTimeApp,
};
