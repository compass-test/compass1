import { defineMessages } from 'react-intl';

export default defineMessages({
  issuesInPlan: {
    id: 'jira.portfolio-plan-wizard.common-issue-count.issues-in-plan',
    defaultMessage: 'Issues in plan',
  },
});
