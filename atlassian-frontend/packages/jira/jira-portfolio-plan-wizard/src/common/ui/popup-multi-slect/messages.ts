// @flow strict-local

import { defineMessages } from 'react-intl';

export default defineMessages({
  excludedFromPlan: {
    id:
      'jira-portfolio-plan-wizard.common-popup-multi-select.excluded-from-plan',
    defaultMessage: 'Excluded from plan',
    description: 'Excluded from plan text',
  },
});
