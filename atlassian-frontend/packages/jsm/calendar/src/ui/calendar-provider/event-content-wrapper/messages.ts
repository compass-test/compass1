import { defineMessages } from 'react-intl';

export default defineMessages({
  noTitle: {
    id: 'jsm.calendar.calendar-provider.event-content-wrapper.no-title',
    description:
      'Placeholder text used as an event title when no title is provided.',
    defaultMessage: '(No title)',
  },
});
