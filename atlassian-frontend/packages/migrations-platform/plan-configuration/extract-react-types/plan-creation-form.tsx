import type { Props } from '../src/ui/plan-creation-form/main';

// This only exists to expose the props for documentation.
// This is necessary because @atlaskit/docs Props doesn't currently work well with FC.
export default (_: Props) => {
  return null;
};
