import { defineMessages } from 'react-intl';

export default defineMessages({
  choiceButtonText: {
    id:
      'com.atlassian.migrations-platform.plan-configuration.migration-field.choice-button-text',
    defaultMessage: 'Choose {destination}',
    description: 'Text for choice button that shows migration gateway modal',
  },
});
