import { defineMessages } from 'react-intl';

export default defineMessages({
  migrationFieldText: {
    id:
      'com.atlassian.migrations-platform.plan-configuration.migration-field.field-text',
    defaultMessage: 'Get {productName} Cloud trial',
    description: 'Text for choice button that shows migration gateway modal',
  },
});
