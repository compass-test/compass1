export type EmptyStateContainerProps = {
  /**
    Top and bottom grid padding multiplier.

    Base value of 1 is one standard grid spacing.
   */
  py?: number;
  /**
    Left and right grid padding multiplier.

    Base value of 1 is one standard grid spacing.
   */
  px?: number;
};
