import { defineMessages } from 'react-intl';

const PREFIX = 'ptc.team-work';

export const messages = defineMessages({
  title: {
    id: `${PREFIX}.title`,
    defaultMessage: 'Jira issues assigned to team',
    description: 'The title of the Team Work component',
  },
});
