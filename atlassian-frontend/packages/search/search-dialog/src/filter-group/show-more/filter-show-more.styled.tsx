import styled from '@emotion/styled';
import { grid } from '../../style-utils';

export const FilterShowMoreWrapper = styled.div`
  margin: ${grid.half().px} 0 0 -${grid.px};
`;

export const FilterShowMoreButtonWrapper = styled.div`
  padding: ${grid.half().px} 0;
`;

export const ScrollBlock = styled.div`
  position: fixed;
  left: 0;
  bottom: 0;
  right: 0;
  top: 0;
`;
