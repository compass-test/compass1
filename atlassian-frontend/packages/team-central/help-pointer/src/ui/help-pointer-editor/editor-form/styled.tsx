import styled from '@emotion/styled';

import { token } from '@atlaskit/tokens';

export const CreateHelpPointerFormContainer = styled.div`
  padding: 32px;
  background-color: ${token('color.background.card', 'white')};
`;

export const ErrorPlaceholder = styled.div`
  height: 16px;
`;

export const StyledTopField = styled.div`
  margin-top: 24px;
`;

export const CancelButtonWrapper = styled.div`
  margin-left: 6px;
`;
