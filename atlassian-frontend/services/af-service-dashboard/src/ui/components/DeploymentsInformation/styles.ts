/** @jsx jsx */

import { css } from '@emotion/core';

export { flexJustifyCenter, inlineText, flexGrow } from '../../styles';

export const columnsContainer = css`
  display: flex;
  align-items: flex-start;
  margin: 0 30px;
  overflow-x: scroll;
`;
