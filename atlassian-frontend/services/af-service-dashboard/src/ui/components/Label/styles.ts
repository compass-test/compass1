import { css } from '@emotion/core';

import { bold, inlineText } from '../../styles';

export const marginRight = css`
  ${bold};
  margin-right: 10px;
`;

export const label = css`
  ${bold};
  ${inlineText};
`;
