// We don't need to declare all of @storybook/react, because it's actually already typed
// just the standalone entrypoint
// declare module '@storybook/react';
declare module '@storybook/react/standalone';
